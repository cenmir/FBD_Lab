[project]
name = "fbd-lab"
version = "0.6.0"
description = "Free Body Diagram Laboratory - A PyQt6 canvas for drawing force vectors"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "PyQt6",
]

[tool.uv]
link-mode = "copy"

[project.scripts]
fbd-lab = "main:main"
